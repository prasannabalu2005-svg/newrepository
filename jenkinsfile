pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Jenkins connected to GitHub!'
            }
        }

        stage('Test') {
            steps {
                echo 'Testing successful!'
            }
        }
    }
}
